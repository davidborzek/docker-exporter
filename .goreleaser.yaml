version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: docker-exporter
    main: .
    binary: docker-exporter
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{ .Version }}
    goos:
      - linux
    goarch:
      - amd64
      - arm64

archives:
  - id: docker-exporter
    formats: [tar.gz]
    name_template: "docker-exporter_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: checksums.txt

dockers:
  - image_templates:
      - "ghcr.io/davidborzek/docker-exporter:{{ .Version }}-amd64"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    goarch: amd64
    build_flag_templates:
      - "--platform=linux/amd64"
      - "--label=org.opencontainers.image.source=https://github.com/davidborzek/docker-exporter"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.revision={{ .FullCommit }}"
  - image_templates:
      - "ghcr.io/davidborzek/docker-exporter:{{ .Version }}-arm64"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    goarch: arm64
    build_flag_templates:
      - "--platform=linux/arm64"
      - "--label=org.opencontainers.image.source=https://github.com/davidborzek/docker-exporter"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.revision={{ .FullCommit }}"

docker_manifests:
  - name_template: "ghcr.io/davidborzek/docker-exporter:{{ .Version }}"
    image_templates:
      - "ghcr.io/davidborzek/docker-exporter:{{ .Version }}-amd64"
      - "ghcr.io/davidborzek/docker-exporter:{{ .Version }}-arm64"
  - name_template: "ghcr.io/davidborzek/docker-exporter:latest"
    image_templates:
      - "ghcr.io/davidborzek/docker-exporter:{{ .Version }}-amd64"
      - "ghcr.io/davidborzek/docker-exporter:{{ .Version }}-arm64"

# release-please already creates the GitHub release; goreleaser only attaches
# the built artifacts to it. The changelog is owned by release-please.
release:
  mode: append

changelog:
  disable: true
